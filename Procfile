worker: python -m bot.main

# Note: unlike Heroku, Railway doesn't auto-run a "release" phase from this
# Procfile. Run migrations manually after each deploy that adds a new one:
#   railway run python -m db.migrate
